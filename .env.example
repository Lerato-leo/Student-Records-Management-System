# Environment Variables for Cloud Deployment
# Copy this file to .env and fill in your cloud database credentials
# NEVER commit .env to GitHub - it's protected by .gitignore

# PostgreSQL Database Configuration (for Render/Railway)
DB_HOST=your_cloud_database_host.render.com
DB_PORT=5432
DB_NAME=student_records_db
DB_USER=postgres
DB_PASSWORD='Amor.1960

# Application Settings
ENVIRONMENT=production
DEBUG=False
LOG_LEVEL=INFO

# Security
SECRET_KEY=your_secret_key_here
ALLOWED_HOSTS=localhost,127.0.0.1,your_cloud_domain.com
